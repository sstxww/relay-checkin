# Contributing

欢迎提交新的中转站适配。

提交前请确认：

1. 配置中只包含站点公开网址、接口路径和环境变量名称。
2. 不包含真实令牌、Cookie、用户 ID、邮箱、服务器 IP 或日志。
3. 为新的响应格式补充离线单元测试。
4. 运行 `python3 -m unittest discover -s tests -v`。

若站点兼容现有请求格式，只需在 `sites.json` 增加配置。若鉴权、请求体或响应格式不同，请新增独立适配逻辑，避免破坏已有站点。
